export function crisisMessage(language, culture) {
  // temporary stub
  return {
    en: "You are not alone. Support is available.",
    hi: "आप अकेले नहीं हैं। मदद उपलब्ध है।",
  }[language] || "Support is available.";
}
